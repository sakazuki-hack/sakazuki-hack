frontディレクトリ内にfrontコンテナ用のファイルをすべて格納したかったが、frontディレクトリにdockerfileがあるとnpx nuxi init .コマンドが使えないため(このコマンドを打つディレクトリは空である必要がある。)このコマンドを打つ前まではプロジェクト直下において、nuxtのインストールが終わった後にfront直下に移す

nuxtはデフォルトでlocalhost:3000を使うらしいが、これはapi用コンテナに割り振ってしまったためポート80を使うように修正
PORT=3001 yarn devとポートを指定して起動する。

yarn add axiosこれもfrontコンテナで一度実行させる必要がある。

